node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Generated by `npm run build:wasm-assets` (scripts/build-wasm-assets.mjs):
# the rendercv wheel, bundled Typst packages, font subset, and the
# typst.ts compiler wasm binary. All derived from the Python source tree /
# node_modules -- see the script for why these aren't committed.
/static/wasm

# Re-include src/lib (root .gitignore excludes any lib/ directory)
!src/lib/
